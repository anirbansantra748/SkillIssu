<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillIssu - Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap');

        body {
            background: radial-gradient(circle, #0a0a0a, #080020);
            font-family: 'Orbitron', sans-serif;
            color: #f8f8f8;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow-x: hidden;
        }

        .container {
            width: 85%;
            max-width: 850px;
            background: rgba(15, 15, 30, 0.95);
            border: 2px solid #00ffee;
            box-shadow: 0 0 20px rgba(0, 255, 238, 0.7);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }

        .avatar {
            width: 110px;
            height: 110px;
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 50%;
            background: radial-gradient(circle, #00ffee, #001a2e);
            box-shadow: 0 0 15px #00ffee;
            font-size: 2.2rem;
            font-weight: bold;
            color: #000;
            margin: auto;
        }

        h2, h3 {
            text-shadow: 0 0 8px #00ffee;
            letter-spacing: 1px;
        }

        .stats-container {
            margin-top: 20px;
            text-align: left;
        }

        .bar-container {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .bar {
            height: 24px;
            border-radius: 12px;
            padding-right: 12px;
            font-weight: bold;
            color: #fff;
            position: relative;
            background: rgba(255, 255, 255, 0.1);
            overflow: hidden;
            display: flex;
            align-items: center;
            padding-left: 15px;
        }

        .bar::before {
            content: '';
            position: absolute;
            height: 100%;
            width: var(--width);
            background: linear-gradient(90deg, #00ffee, #0088ff);
            border-radius: 12px;
            transition: width 0.8s ease-in-out;
        }

        .problems {
            margin-top: 25px;
            text-align: left;
            background: rgba(0, 255, 238, 0.15);
            padding: 15px;
            border-radius: 12px;
        }

        .problems li {
            list-style: none;
            padding: 6px;
            font-size: 1.1rem;
        }

        .problems li a {
            text-decoration: none;
            color: #00ffee;
            font-weight: bold;
            transition: 0.3s;
        }

        .problems li a:hover {
            color: #ff0;
            text-shadow: 0 0 8px #ff0;
        }

        /* Floating Interactive Pet */
        .pet {
            position: absolute;
            bottom: 20px;
            right: 50px;
            width: 60px;
            height: 60px;
            background: url('https://media.tenor.com/-g-bRmUm09EAAAAi/cat.gif') no-repeat center;
            background-size: contain;
            animation: float 3s infinite ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="avatar">⚡</div>
        <h2><%= username %></h2>
        <div>🌍 Country: <%= Country %></div>
        <div>🏆 Points: <%= points %></div>
        <div>📊 Total Submissions: <%= totalSubmissions %></div>

        <div class="stats-container">
            <h3>Problem Solving Stats:</h3>
            <div class="bar-container">
                <div class="bar" style="--width: calc(<%= stats.easy %> * 10%)">Easy: <%= stats.easy %></div>
                <div class="bar" style="--width: calc(<%= stats.medium %> * 10%)">Medium: <%= stats.medium %></div>
                <div class="bar" style="--width: calc(<%= stats.hard %> * 10%)">Hard: <%= stats.hard %></div>
            </div>
        </div>

        <div class="problems">
            <h3>All Solved Problems:</h3>
            <ul>
                <% problemsSolved.forEach(problem => { %>
                    <li>✅ <a href="/problems/<%= problem.problemId %>" target="_blank"><%= problem.title %></a></li>
                <% }); %>
            </ul>
        </div>
    </div>

    <div class="pet"></div> <!-- Floating Pet -->
</body>
</html>
